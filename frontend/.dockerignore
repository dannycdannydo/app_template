# Keep the build context small: dependencies, build output, and test artefacts
# are never copied into the image (the build stage installs its own copy).
node_modules/
dist/
coverage/
playwright-report/
test-results/
e2e/
*.tsbuildinfo
